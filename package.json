{
  "name": "rhine-media-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "sass --no-source-map scss/style.scss:css/style.css",
    "watch:css": "sass --no-source-map --watch scss/style.scss:css/style.css"
  },
  "devDependencies": {
    "sass": "^1.94.2"
  }
}
